{
  "name": "letter-drop",
  "version": "0.1.0",
  "private": true,
  "description": "A word game: drop letters through shorter and shorter words.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test test/*.test.js"
  }
}
